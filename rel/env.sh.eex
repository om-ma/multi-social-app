#!/bin/sh

# Sets and samples the release environment for the current release mode.

# Use short names for Erlang distribution (enables remote console)
export RELEASE_DISTRIBUTION="${RELEASE_DISTRIBUTION:-sname}"
export RELEASE_NODE="${RELEASE_NODE:-social_app}"
